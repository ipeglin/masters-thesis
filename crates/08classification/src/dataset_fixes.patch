--- crates/08classification/src/dataset.rs
+++ crates/08classification/src/dataset.rs
@@ -871,8 +871,8 @@
     
     let spec = FeatureSpec {
         source,
-        aggregation: FeatureAggregation::WholeBand,
-        subgroup: FeatureSubgroup::PerRoi,
+        subgroup: FeatureSubgroup::WholeBand,
+        aggregation: FeatureAggregation::Concat,
     };
     
     for subject in subject_ids {
@@ -946,26 +946,16 @@
             continue;
         }
 
-        let spec = FeatureSpec {
-            source,
-            aggregation: FeatureAggregation::PerBlock,
-            subgroup: FeatureSubgroup::PerRoi,
-        };
-
         for file in files {
-            let meta = match read_roi_metadata(&file) {
-                 Ok(m) => m,
-                 Err(_) => continue
-            };
             
-            let block_indices = match face_only {
-                true => detect_block_indices(&meta, "face"),
-                false => vec![], // simplified
-            };
+            let all_blocks = list_feature_block_indices(&file, source);
 
-            for (logical_idx, actual_block) in block_indices.iter().enumerate() {
-                 let dataset_path = spec.h5_path(Some(*actual_block));
-                 if let Ok(data) = hdf5_io::read_dataset_1d::<f32>(&file, &dataset_path) {
+            for (logical_idx, actual_block) in all_blocks.iter().enumerate() {
+                 if face_only {
+                      if let Some(t) = read_block_trial_type(&file, *actual_block) {
+                          if !TrialType::Face.matches(&t) { continue; }
+                      } else { continue; }
+                 }
+                 let spec = FeatureSpec {
+                      source,
+                      subgroup: FeatureSubgroup::Block(*actual_block),
+                      aggregation: FeatureAggregation::Concat,
+                 };
+                 if let Ok(data) = read_feature_vector(&file, &spec) {
                     let n_rois = 28;
                     let dim = data.len() / n_rois;
                     let entry = block_map.entry(logical_idx).or_insert_with(|| (Vec::new(), Vec::new(), Vec::new()));
